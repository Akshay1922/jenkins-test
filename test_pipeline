pipeline{
        agent any
        environment {
                secret = credentials('Test')
                TEST_ENV = 'dev'
        }
        stages {
                stage('Examole stage 1') {
                        steps {
                                sh 'echo $secret'
                                sh 'echo $TEST_ENV'
                        }
                }
        }
}